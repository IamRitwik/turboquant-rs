//! turboquant-rs — TurboQuant KV-cache compression in Rust
//!
//! Run `cargo run --example demo --release` for the full benchmark demo.

fn main() {
    println!("turboquant-rs: Google TurboQuant KV-cache compression in Rust");
    println!("Run: cargo run --example demo --release");
    println!("Run: cargo bench");
    println!("Run: cargo test");
}
